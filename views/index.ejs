 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EPMS - Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-image: url( https://images.pexels.com/photos/4110100/pexels-photo-4110100.jpeg);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      height: 100vh;
      margin: 0;
      padding: 0;
      width: 100%;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
      color: white;
      overflow-x: hidden;
    }
    .hero {
      text-align: center;
      animation: fadeInUp 1s ease forwards;
      opacity: 0;
      transform: translateY(20px);
    }
    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .hero h1 {
      color: #642102;
      font-size: 3rem;
      font-weight: bold;
      animation: fadeInScale 1s ease forwards;
    }
    @keyframes fadeInScale {
      0% { opacity: 0; transform: scale(0.9); }
      100% { opacity: 1; transform: scale(1); }
    }
    .lead {
      color: #504802;
      font-size: 1.25rem;
      margin-bottom: 2rem;
    }
    .btn-custom {
      background: #504802;
      color: #fdfcfb;
      font-weight: bold;
      padding: 0.75rem 2rem;
      border-radius: 50px;
      transition: all 0.3s ease;
    }
    .btn-custom:hover {
      background: #4facfe;
      color: white;
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
    footer {
      position: fixed;
      bottom: 0;
      width: 100%;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
      color: rgba(255,255,255,0.8);
    }
  </style>
</head>
<body>
  
  <div class="hero">
    <h1>Welcome to EPMS</h1>
    <p class="lead">Employee & Project Management System</p>
    <% if (!currentUser) { %>
      <a class="btn btn-custom" href="/auth/login">Login</a>
    <% } else { %>
      <a class="btn btn-custom" href="<%= currentUser.role === 'admin' ? '/admin/dashboard' : '/user/dashboard' %>">
        Go to Dashboard
      </a>
    <% } %>
  </div>

  <footer>
    &copy; <%= new Date().getFullYear() %> EPMS. All rights reserved.
  </footer>

</body>
</html>
